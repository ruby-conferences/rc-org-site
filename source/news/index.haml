%article#news
  %h1.extruded Event News

  %dl
    - blog.articles.each do |article|
      %dt
        %time= article.date.strftime('%b %e %Y')
        = link_to article.title, article.url
      %dd= article.body
